{
  "name": "@teleplatform/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "build": "echo 'no build'",
    "lint": "eslint . || true",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1"
  },
  "devDependencies": {
    "prisma": "^5.19.1",
    "typescript": "^5.5.4"
  }
}
